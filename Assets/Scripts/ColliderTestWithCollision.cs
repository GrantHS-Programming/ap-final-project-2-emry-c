using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColliderTestWithCollision : MonoBehaviour
{
    /**
    private Tilemap map;
    private Tilebase tile;
    // Update is called once per frame
    void Update()
    {
        
    }

    private void OnCollisonEnter2D(Collision2D thing) {
        Tilemap map = thing.GetComponentInParent<Tilemap>();
        map.WorldToCell(thing.gameObject.transform.position);
        tile = tileMap.GetTile(map.WorldToCell(thing.gameObject.transform.position));
        print(tile);

    }
    */



}
